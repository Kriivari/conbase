<% Orderitem.content_columns.each do |column| %>
<p>
  <b><%= column.human_name %>:</b> <%=h @orderitem.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @orderitem %> |
<%= link_to 'Back', :action => 'list' %>
