<% Personeventattribute.content_columns.each do |column| %>
<p>
  <b><%= column.human_name %>:</b> <%=h @personeventattribute.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @personeventattribute %> |
<%= link_to 'Back', :action => 'list' %>
