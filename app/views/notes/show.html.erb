<% Note.content_columns.each do |column| %>
<p>
  <b><%= column.human_name %>:</b> <%=h @note.send(column.name) %>
</p>
<% end %>

<%= link_to 'Muokkaa', :action => 'edit', :id => @note %> |
<%= link_to 'Listaan', :action => 'list' %>
