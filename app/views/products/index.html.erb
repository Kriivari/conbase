<h1>Myyntituotteet</h1>

<table>
  <tr>
    <th>Nimi</th>
    <th>Myytävissä</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= product.active %></td>
    <td><%= link_to 'Näytä', product %></td>
    <td><%= link_to 'Muokkaa', edit_product_path(product) %></td>
    <td><%= link_to 'Poista', product, :method => :delete, :data => { :confirm => 'Oletko varma?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Uusi myyntituote', new_product_path %>
<%= link_to 'Tuotetyypit', product_types_path %>
