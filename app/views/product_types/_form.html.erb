<%= form_for(@product_type) do |f| %>
  <% if @product_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_type.errors.count, "error") %> prohibited this product_type from being saved:</h2>

      <ul>
      <% @product_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr><td>Myyntituote:</td><td><%= collection_select(:product_type, :product_id, Product.all, :id, :name) %></td></tr>
    <tr><td>Nimi:</td><td><%= f.text_field :name %></td></tr>
    <tr><td>Englanninkielinen nimi:</td><td><%= f.text_field :nameen %></td></tr>
    <tr><td>Hinta:</td><td><%= f.text_field :price %></td></tr>
    <tr><td>Toisen ja useamman kappaleen hinta:</td><td><%= f.text_field :secondprice %></td></tr>
    <tr><td>Myytävissä:</td><td><%= f.check_box :active %></td></tr>
    <tr><td><%= f.submit "Talleta" %></td></tr>
  </table>
<% end %>
