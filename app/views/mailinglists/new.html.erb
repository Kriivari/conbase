<h1>New mailinglist</h1>

<%= form_for( @mailinglist, :url => { :action => 'create' } ) do |f| %>
  <%= render :partial => 'form' %>
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', :action => 'list' %>
