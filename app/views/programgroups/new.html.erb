<h1>New programgroup</h1>

<%= form_for( @programgroup, :url => { :action => 'create' } ) do |f| %>
  <%= render :partial => 'form' %>
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', :action => 'list' %>
